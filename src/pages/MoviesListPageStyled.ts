import styled from "styled-components";

const MoviesListPageStyled = styled.main`
  .header__title {
    border: solid;
    border-radius: 30px;
    padding: 20px;
  }
`;

export default MoviesListPageStyled;
